// Fill these in before running, or create `helium.config.local.ts` with the
// same shape — it is gitignored and takes precedence, so your API key never
// lands in a commit.
export default {
  apiKey: 'REPLACE_ME',
  trigger: 'REPLACE_ME',
};
